apiVersion: v1
kind: Service
metadata:
  labels:
    scrumdapp.app/name: {{name}}
  name: {{name}}
  #if exists namespace
  namespace: {{namespace}}
  #endif
spec:
  ports:
    - name: {{port_name}}
      port: {{port}}
      targetPort: {{port}}
  selector:
    scrumdapp.app/name: {{name}}
